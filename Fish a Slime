-- LocalScript

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TextService = game:GetService("TextService")

local player = Players.LocalPlayer

local tituloGUI = "Fish a Slime"

local textSize = TextService:GetTextSize(
	tituloGUI,
	16,
	Enum.Font.GothamBold,
	Vector2.new(math.huge, math.huge)
)

local normalWidth = math.max(250, textSize.X + 140)

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = tituloGUI
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.fromOffset(normalWidth, 200)
frame.Position = UDim2.new(0.5, -normalWidth/2, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(35,35,35)
frame.BorderSizePixel = 0
frame.Parent = gui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0,8)
frameCorner.Parent = frame

-- Barra superior
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1,0,0,30)
topBar.BackgroundColor3 = Color3.fromRGB(30,30,30)
topBar.BorderSizePixel = 0
topBar.Parent = frame

local topCorner = Instance.new("UICorner")
topCorner.CornerRadius = UDim.new(0,8)
topCorner.Parent = topBar

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,-120,1,0)
title.Position = UDim2.fromOffset(10,0)
title.BackgroundTransparency = 1
title.Text = tituloGUI
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextTruncate = Enum.TextTruncate.AtEnd
title.Parent = topBar

-- Botões da barra
local function createTopButton(text, posX)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0,30,1,0)
	btn.Position = UDim2.new(1,posX,0,0)
	btn.BackgroundTransparency = 1
	btn.Text = text
	btn.TextColor3 = Color3.new(1,1,1)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 16
	btn.Parent = topBar
	return btn
end

local minimizeBtn = createTopButton("-", -90)
local maximizeBtn = createTopButton("▢", -60)
local closeBtn = createTopButton("X", -30)

-- Área dos botões
local content = Instance.new("Frame")
content.BackgroundTransparency = 1
content.Position = UDim2.fromOffset(10,40)
content.Size = UDim2.new(1,-20,1,-50)
content.Parent = frame

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0,8)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.Parent = content

local function createButton(text)
	local button = Instance.new("TextButton")
	button.Size = UDim2.new(1,0,0,35)
	button.BackgroundColor3 = Color3.fromRGB(55,55,55)
	button.BorderSizePixel = 0
	button.Text = text
	button.TextColor3 = Color3.new(1,1,1)
	button.Font = Enum.Font.Gotham
	button.TextSize = 14
	button.Parent = content

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0,6)
	corner.Parent = button

	return button
end

-- Botões
local AutoCollectMoneyBtn = createButton("Auto Collect Money")
local AutoCatchSlimeBtn = createButton("Auto Catch(Wait for it to be caught)")
-- local summonBtn = createButton("Executar Summon")

-- Exemplo de adicionar mais botões:
-- local summonBtn1 = createButton("Executar Summon 2")
-- local summonBtn2 = createButton("Executar Summon 3")

-- Créditos
local creditLabel = Instance.new("TextLabel")
creditLabel.Size = UDim2.new(1,0,0,20)
creditLabel.BackgroundTransparency = 1
creditLabel.Text = "by iagocno"
creditLabel.TextColor3 = Color3.fromRGB(180,180,180)
creditLabel.TextTransparency = 0.2
creditLabel.Font = Enum.Font.LuckiestGuy
creditLabel.TextSize = 16
creditLabel.Parent = content

local function UpdateFrameHeight()

	local height =
		layout.AbsoluteContentSize.Y
		+ 60

	frame.Size = UDim2.fromOffset(
		frame.Size.X.Offset,
		height
	)
end

UpdateFrameHeight()

layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	UpdateFrameHeight()
end)

-- Auto Collect Money
AutoCollectMoneyBtn.MouseButton1Click:Connect(function()
	for i = 1, 30 do
		pcall(function()
			ReplicatedStorage.Remotes.Claim:InvokeServer(i)
		end)

		task.wait(0.5)
	end
end)

-- Auto Catch Slime
AutoCatchSlimeBtn.MouseButton1Click:Connect(function()
	game:GetService("ReplicatedStorage").Remotes.OnCast:InvokeServer(1)

	task.wait(1)

	game:GetService("ReplicatedStorage").Remotes.onSwimComplete:InvokeServer(false, false)

end)

-- Caixa de texto
-- Linha Buy Speed
local BuySpeedRow = Instance.new("Frame")
BuySpeedRow.Size = UDim2.new(1,0,0,35)
BuySpeedRow.BackgroundTransparency = 1
BuySpeedRow.Parent = content

local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(0.8,0,1,0)
SpeedBox.Position = UDim2.new(0,0,0,0)
SpeedBox.PlaceholderText = "Buy Speed"
SpeedBox.Text = ""
SpeedBox.Font = Enum.Font.Gotham
SpeedBox.TextSize = 14
SpeedBox.BackgroundColor3 = Color3.fromRGB(45,45,45)
SpeedBox.TextColor3 = Color3.new(1,1,1)
SpeedBox.Parent = BuySpeedRow

local BoxCorner = Instance.new("UICorner")
BoxCorner.CornerRadius = UDim.new(0,6)
BoxCorner.Parent = SpeedBox

local ConfirmButton = Instance.new("TextButton")
ConfirmButton.Size = UDim2.new(0.18,0,1,0)
ConfirmButton.Position = UDim2.new(0.82,0,0,0)
ConfirmButton.Text = "✓"
ConfirmButton.Font = Enum.Font.GothamBold
ConfirmButton.TextSize = 18
ConfirmButton.BackgroundColor3 = Color3.fromRGB(60,120,60)
ConfirmButton.TextColor3 = Color3.new(1,1,1)
ConfirmButton.Parent = BuySpeedRow

local ConfirmCorner = Instance.new("UICorner")
ConfirmCorner.CornerRadius = UDim.new(0,6)
ConfirmCorner.Parent = ConfirmButton

ConfirmButton.MouseButton1Click:Connect(function()

	local value = tonumber(SpeedBox.Text)

	if not value then
		return
	end

	value = math.floor(value)

	ReplicatedStorage.Remotes.BuySpeed:InvokeServer(value)

end)

-- ALT DIREITO
local guiVisible = true

UserInputService.InputBegan:Connect(function(input, gpe)
	if gpe then return end

	if input.KeyCode == Enum.KeyCode.RightAlt then
		guiVisible = not guiVisible
		frame.Visible = guiVisible
	end
end)

local minimized = false
local maximized = false

local storedSize = frame.Size
local normalSize = frame.Size

minimizeBtn.MouseButton1Click:Connect(function()

	minimized = not minimized

	if minimized then

		storedSize = frame.Size

		content.Visible = false

		TweenService:Create(
			frame,
			TweenInfo.new(0.2),
			{
				Size = UDim2.fromOffset(
					storedSize.X.Offset,
					30
				)
			}
		):Play()

	else

		content.Visible = true

		TweenService:Create(
			frame,
			TweenInfo.new(0.2),
			{
				Size = storedSize
			}
		):Play()

	end
end)

maximizeBtn.MouseButton1Click:Connect(function()

	if minimized then
		minimized = false
		content.Visible = true

		TweenService:Create(
			frame,
			TweenInfo.new(0.2),
			{
				Size = storedSize
			}
		):Play()

		return
	end

	maximized = not maximized

	local targetSize

if maximized then

	local contentHeight =
		layout.AbsoluteContentSize.Y + 60

	targetSize = UDim2.fromOffset(
		math.max(normalWidth, 450),
		contentHeight
	)

else

	targetSize = UDim2.fromOffset(
		normalWidth,
		layout.AbsoluteContentSize.Y + 60
	)

end

	TweenService:Create(
		frame,
		TweenInfo.new(0.2),
		{
			Size = targetSize
		}
	):Play()
end)

closeBtn.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

-- Drag
local dragging = false
local dragInput
local dragStart
local startPos

topBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then

		dragging = true
		dragStart = input.Position
		startPos = frame.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

topBar.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement then
		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)

	if dragging and input == dragInput then

		local delta = input.Position - dragStart

		frame.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)
